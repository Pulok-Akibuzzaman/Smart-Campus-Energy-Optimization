{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'GridWise uses Vercel\'s auto-detected Python framework preset; no build step needed'",
  "installCommand": "pip install -r requirements.txt",
  "framework": null,
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "memory": 2048,
      "excludeFiles": "{tests/**,docs/**,**/*.md,publish.sh,Procfile,railway.toml,Dockerfile,docker-compose.yml,pytest.ini,.dockerignore,.gitignore}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
